@import '../css/base.styl'

* {
    padding 0
    margin 0
    box-sizing border-box
}

body,
html {
    width 100%
    height 100%
}

:global {
    #root {
        width 100%
        height: 100%;
    }
}

.app {
    margin: 0 auto;
    width: 100%;
    height: 100vh;
    max-width: 100%;
    max-height: 100vh;
}
